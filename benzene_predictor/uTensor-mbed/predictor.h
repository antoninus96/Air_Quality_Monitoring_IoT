#include "model.hpp"  //gernerated model file
#include "tensor.hpp"  //useful tensor classes

float predict(float* values); // takes an array of three values (CO ,Temp and RH) and predicts benzene levels
